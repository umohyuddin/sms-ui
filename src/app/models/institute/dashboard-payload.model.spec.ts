import { DashboardPayload } from './dashboard-payload.model';

describe('DashboardPayload', () => {
  it('should create an instance', () => {
    expect(new DashboardPayload()).toBeTruthy();
  });
});
